SELECT l.name AS leser, b.titel AS buch, a.ausgeliehen_am FROM ausleihen a JOIN leser l ON a.leser_id = l.id JOIN buecher b ON a.buch_id = b.id WHERE a.zurueck_am IS NULL ORDER BY a.ausgeliehen_am, l.name;
